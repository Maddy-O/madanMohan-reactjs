export interface Product {
  avatar: string;
  category: string;
  description: string;
  developerEmail: string;
  name: string;
  price: number;
  _id: number;
}

export interface Categories {
  name: string;
  _id: number;
}
